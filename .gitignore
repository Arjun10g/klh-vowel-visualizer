# Python
.venv/
__pycache__/
**/__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Node / Vite
frontend/node_modules/
frontend/dist/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local Claude config (per-machine)
.claude/

# Logs / scratch
*.log
/tmp/

# Local planning notes — stay out of the repo
*_PLAN.md

# Raw CSV is regenerated into parquet via backend.build_parquet.
# The parquet (5.5 MB) is what we ship — 16× smaller than the CSV (88 MB),
# small enough to avoid LFS on GitHub and HF Spaces.
all_data_18Nov2023.csv
